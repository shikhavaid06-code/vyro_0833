import type { MetadataRoute } from 'next';

// ✅ PWA manifest — Next.js's App Router auto-discovers this file, generates
// /manifest.webmanifest, and injects the <link rel="manifest"> tag into
// every page automatically. No extra config or package needed.
export default function manifest(): MetadataRoute.Manifest {
  return {
    name: 'CRÉO — Create Viral Content with AI',
    short_name: 'CRÉO',
    description: 'Turn a raw idea into viral titles, hooks, and scripts in seconds.',
    start_url: '/',
    display: 'standalone',
    background_color: '#080812',
    theme_color: '#080812',
    icons: [
      { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png' },
      { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png' },
      { src: '/icons/icon-maskable-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
    ],
  };
}
