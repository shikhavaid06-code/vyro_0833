'use client';

import React, { useEffect, useMemo, useState } from 'react';
import { useRouter } from 'next/navigation';
import { Home as HomeIcon, Zap, Brain, Lightbulb, FileText, Star, BarChart3, Calendar, Users, Settings, Search, Bell, ChevronRight, TrendingUp, Sparkles, Plus, ArrowUpRight, Lock, Flame, Check, Target, Globe2 } from 'lucide-react';
import AppLogo from '@/components/ui/AppLogo';
import { supabase } from '@/lib/supabase';

interface SavedChat { id: string; title: string; preview?: string; time: string; platform: string; generated?: number; }
interface UserState { name?: string; email?: string; plan?: string }
const NAV = [
  { label: 'Home', icon: HomeIcon, href: '/home', active: true }, { label: 'Create', icon: Zap, href: '/main-app-chat-interface' },
  { label: 'Ideas', icon: Lightbulb, href: '/main-app-chat-interface' }, { label: 'Content', icon: FileText, href: '/main-app-chat-interface' },
  { label: 'Vault', icon: Star, href: '/main-app-chat-interface' }, { label: 'Brain', icon: Brain, href: '/main-app-chat-interface' },
  { label: 'Analytics', icon: BarChart3, href: '/main-app-chat-interface' }, { label: 'Calendar', icon: Calendar, href: '/main-app-chat-interface' },
  { label: 'Teams', icon: Users, href: '/upgrade' },
];
function greeting() { const h = new Date().getHours(); return h < 12 ? 'Good morning' : h < 18 ? 'Good afternoon' : 'Good evening'; }
function StatCard({ label, value, sub, icon: Icon }: { label: string; value: string; sub: string; icon: React.ElementType }) {
  return <div className="creo-surface rounded-xl p-4 min-w-0"><div className="flex items-center justify-between mb-3"><span className="creo-caption text-creo-text-muted">{label}</span><Icon size={14} className="text-creo-text-muted" /></div><p className="text-xl font-semibold text-creo-text-primary tracking-tight">{value}</p><p className="creo-caption text-creo-text-muted mt-1">{sub}</p></div>;
}
function EmptyIntelligence({ title, body, action, onClick, icon: Icon }: { title: string; body: string; action: string; onClick: () => void; icon: React.ElementType }) {
  return <div className="creo-surface rounded-xl p-4"><div className="flex items-start gap-3"><div className="w-8 h-8 rounded-lg bg-creo-primary/10 border border-creo-primary/20 flex items-center justify-center flex-shrink-0"><Icon size={14} className="text-creo-primary" /></div><div className="min-w-0"><p className="creo-body font-semibold text-creo-text-primary">{title}</p><p className="creo-caption text-creo-text-muted leading-relaxed mt-1">{body}</p><button onClick={onClick} className="creo-caption text-creo-primary flex items-center gap-1 mt-3 hover:opacity-80">{action}<ArrowUpRight size={11} /></button></div></div></div>;
}
export default function HomeScreen() {
  const router = useRouter(); const [user, setUser] = useState<UserState>({}); const [chats, setChats] = useState<SavedChat[]>([]); const [genToday, setGenToday] = useState(0); const [streak, setStreak] = useState(0); const [lastGenDate, setLastGenDate] = useState<string | null>(null); const [analyticsConnected, setAnalyticsConnected] = useState(false); const [metrics, setMetrics] = useState<any>(null);
  useEffect(() => {
    try { setUser(JSON.parse(localStorage.getItem('creo_current_user') || '{}')); } catch {} try { setChats(JSON.parse(localStorage.getItem('creo_chat_history') || '[]')); } catch {}
    try { const today = new Date().toISOString().slice(0, 10); if (localStorage.getItem('creo_gen_count_date') === today) setGenToday(parseInt(localStorage.getItem('creo_gen_count') || '0', 10)); setStreak(parseInt(localStorage.getItem('creo_streak') || '0', 10)); } catch {}
    (async () => { try { const { data: { session } } = await supabase.auth.getSession(); if (!session) return; const h = { Authorization: `Bearer ${session.access_token}` }; const sr = await fetch('/api/streak', { headers: h }); if (sr.ok) { const d = await sr.json(); if (typeof d.streakCount === 'number') setStreak(d.streakCount); setLastGenDate(d.lastGenDate || null); } const ar = await fetch('/api/analytics', { headers: h }); if (ar.ok) { const d = await ar.json(); if (d?.metrics && d.metrics.totalViews != null) { setMetrics(d.metrics); setAnalyticsConnected(true); } } } catch {} })();
  }, []);
  const plan = user.plan || 'free'; const limit = plan === 'ultra' ? Infinity : plan === 'pro' ? 100 : 3; const firstName = (user.name || 'Creator').split(' ')[0]; const generationLabel = limit === Infinity ? `${genToday}` : `${genToday} / ${limit}`; const progress = limit === Infinity ? 0 : Math.min(100, (genToday / limit) * 100); const recent = chats.slice(0, 4); const streakLabel = useMemo(() => streak ? `${streak} day${streak === 1 ? '' : 's'} active` : 'Start your first streak', [streak, lastGenDate]);
  return <div className="min-h-screen bg-creo-bg text-creo-text-primary flex">
    <aside className="hidden lg:flex w-56 flex-shrink-0 flex-col border-r border-creo-border bg-creo-bg"><div className="h-16 px-4 flex items-center gap-2 border-b border-creo-border"><AppLogo size={24} /><div><p className="text-sm font-semibold tracking-wide">CRÉO</p><p className="creo-caption text-creo-text-muted">Creator intelligence</p></div></div><nav className="flex-1 p-2 space-y-0.5">{NAV.map(({ label, icon: Icon, href, active }) => <button key={label} onClick={() => router.push(href)} className={`w-full flex items-center gap-3 px-3 py-2.5 rounded-lg text-left creo-body transition-colors ${active ? 'bg-creo-primary/10 text-creo-primary border border-creo-primary/20' : 'text-creo-text-secondary hover:text-creo-text-primary hover:bg-creo-surface'}`}><Icon size={15} /><span className="flex-1">{label}</span>{label === 'Teams' && <span className="creo-caption text-creo-text-muted">Soon</span>}</button>)}</nav><div className="p-3 border-t border-creo-border space-y-2"><button onClick={() => router.push('/settings')} className="w-full flex items-center gap-3 px-3 py-2 rounded-lg text-creo-text-secondary hover:text-creo-text-primary hover:bg-creo-surface"><Settings size={15} /><span className="creo-body">Settings</span></button><div className="creo-surface rounded-xl p-3"><div className="flex items-center justify-between mb-1"><span className="creo-caption text-creo-text-muted uppercase tracking-wider">{plan} plan</span>{plan === 'free' && <button onClick={() => router.push('/upgrade')} className="creo-caption text-creo-primary">Upgrade</button>}</div><p className="text-sm font-medium text-creo-text-primary">{limit === Infinity ? 'Unlimited' : generationLabel}</p><p className="creo-caption text-creo-text-muted mt-1">generations today</p>{limit !== Infinity && <div className="h-1 bg-creo-surface3 rounded-full mt-2 overflow-hidden"><div className="h-full bg-creo-primary rounded-full" style={{ width: `${progress}%` }} /></div>}</div></div></aside>
    <main className="flex-1 min-w-0"><header className="h-16 border-b border-creo-border flex items-center gap-3 px-4 sm:px-6 sticky top-0 z-20 bg-creo-bg/95 backdrop-blur-md"><div className="hidden md:flex items-center gap-2 creo-surface rounded-lg px-3 py-2 w-64 text-creo-text-muted creo-caption"><Search size={13} /> Search your workspace...</div><div className="flex-1" /><button className="w-9 h-9 rounded-lg border border-creo-border text-creo-text-muted hover:text-creo-text-primary hover:bg-creo-surface flex items-center justify-center" aria-label="Notifications"><Bell size={15} /></button><button onClick={() => router.push('/settings')} className="w-9 h-9 rounded-full bg-creo-surface border border-creo-border flex items-center justify-center text-sm font-medium text-creo-primary">{firstName[0]?.toUpperCase()}</button></header>
      <div className="p-4 sm:p-6 xl:p-8 max-w-[1500px] mx-auto space-y-5"><section className="flex flex-col sm:flex-row sm:items-end justify-between gap-4"><div><p className="creo-caption text-creo-text-muted uppercase tracking-[0.18em] mb-2">Workspace</p><h1 className="creo-h2 text-creo-text-primary">{greeting()}, {firstName}.</h1><p className="creo-body text-creo-text-muted mt-1">Let's build something exceptional today.</p></div><button onClick={() => router.push('/main-app-chat-interface')} className="creo-btn-primary text-white px-4 py-2.5 rounded-lg creo-body font-medium flex items-center gap-2 self-start sm:self-auto"><Plus size={15} /> New content</button></section>
        <section className="grid grid-cols-2 xl:grid-cols-4 gap-3"><StatCard label="Generations" value={generationLabel} sub="today" icon={Zap} /><StatCard label="Streak" value={`${streak}`} sub={streakLabel} icon={Flame} /><StatCard label="Content" value={`${chats.length}`} sub="saved workspace items" icon={FileText} /><StatCard label="Views" value={analyticsConnected ? Number(metrics.totalViews || 0).toLocaleString() : '—'} sub={analyticsConnected ? 'connected data' : 'connect analytics'} icon={TrendingUp} /></section>
        <section><div className="flex items-center justify-between mb-3"><div><h2 className="creo-h3">Today's intelligence</h2><p className="creo-caption text-creo-text-muted mt-0.5">Only insights CRÉO can currently support with your data.</p></div><span className="creo-caption text-creo-text-muted">{analyticsConnected ? 'Data connected' : 'Needs data'}</span></div><div className="grid grid-cols-1 md:grid-cols-3 gap-3"><EmptyIntelligence title="Content opportunity" body="CRÉO will surface opportunities once audience and performance data are connected. No invented trend claims." action={analyticsConnected ? 'Review analytics' : 'Connect data'} onClick={() => router.push('/main-app-chat-interface')} icon={Target} /><EmptyIntelligence title="Audience insight" body="Audience geography and behavior belong here after a connector supplies real observations." action="Open analytics" onClick={() => router.push('/main-app-chat-interface')} icon={Globe2} /><EmptyIntelligence title="Performance tip" body="Your actual content history can become recommendations. Until then, this stays intentionally empty." action="Create content" onClick={() => router.push('/main-app-chat-interface')} icon={Sparkles} /></div></section>
        <section className="grid grid-cols-1 xl:grid-cols-[1.4fr_.9fr] gap-4"><div className="creo-surface rounded-xl p-4"><div className="flex items-center justify-between mb-4"><div><h2 className="creo-h3">Continue working on</h2><p className="creo-caption text-creo-text-muted mt-0.5">Your actual saved work.</p></div><button onClick={() => router.push('/main-app-chat-interface')} className="creo-caption text-creo-primary flex items-center gap-1">View all <ChevronRight size={11} /></button></div>{recent.length ? <div className="grid sm:grid-cols-2 gap-3">{recent.map((chat) => <button key={chat.id} onClick={() => router.push('/main-app-chat-interface')} className="text-left rounded-lg border border-creo-border bg-creo-bg/50 p-3 hover:border-creo-border-strong transition-colors"><div className="flex items-center justify-between gap-3"><span className="creo-caption text-creo-text-muted">{chat.platform || 'Content'}</span><span className="creo-caption text-creo-text-muted">{chat.time}</span></div><p className="creo-body font-medium text-creo-text-primary mt-3 line-clamp-2">{chat.title || 'Untitled content'}</p><div className="h-1 bg-creo-surface3 rounded-full mt-4"><div className="h-full bg-creo-primary rounded-full" style={{ width: `${Math.min(100, (chat.generated || 1) * 20)}%` }} /></div></button>)}</div> : <div className="py-10 text-center border border-dashed border-creo-border rounded-lg"><FileText size={18} className="mx-auto text-creo-text-muted mb-2" /><p className="creo-body text-creo-text-secondary">Nothing here yet.</p><p className="creo-caption text-creo-text-muted mt-1">Create your first piece and it will appear here.</p></div>}</div><div className="creo-surface rounded-xl p-4"><div className="flex items-center justify-between mb-4"><div><h2 className="creo-h3">Creator Brain</h2><p className="creo-caption text-creo-text-muted mt-0.5">Your durable creative context.</p></div><button onClick={() => router.push('/main-app-chat-interface')} className="creo-caption text-creo-primary">Open Brain</button></div><div className="border border-creo-border rounded-lg p-4 bg-creo-bg/40"><div className="flex items-center gap-3"><div className="w-9 h-9 rounded-lg bg-creo-primary/10 border border-creo-primary/20 flex items-center justify-center"><Brain size={16} className="text-creo-primary" /></div><div><p className="creo-body font-medium text-creo-text-primary">Build your content system</p><p className="creo-caption text-creo-text-muted">Niche, voice, goals and patterns.</p></div></div><div className="grid grid-cols-2 gap-2 mt-4"><div className="creo-surface rounded-lg p-2.5"><p className="creo-caption text-creo-text-muted">Voice</p><p className="creo-caption text-creo-text-secondary mt-1">Not captured</p></div><div className="creo-surface rounded-lg p-2.5"><p className="creo-caption text-creo-text-muted">Goals</p><p className="creo-caption text-creo-text-secondary mt-1">Not captured</p></div></div><button onClick={() => router.push('/main-app-chat-interface')} className="w-full mt-3 py-2 rounded-lg border border-creo-border text-creo-text-secondary creo-caption hover:text-creo-text-primary hover:bg-creo-surface">Start building your Brain</button></div></div></section>
        <section className="grid grid-cols-1 lg:grid-cols-3 gap-4"><div className="creo-surface rounded-xl p-4 lg:col-span-2"><div className="flex items-center justify-between mb-4"><div><h2 className="creo-h3">Performance overview</h2><p className="creo-caption text-creo-text-muted mt-0.5">No fake metrics. Connected data only.</p></div><span className="creo-caption text-creo-text-muted">Last 30 days</span></div>{analyticsConnected ? <div className="grid grid-cols-3 gap-3"><StatCard label="Views" value={Number(metrics.totalViews || 0).toLocaleString()} sub="tracked" icon={TrendingUp} /><StatCard label="Engagement" value={metrics.avgEngagementRate != null ? `${metrics.avgEngagementRate}%` : '—'} sub="average rate" icon={Sparkles} /><StatCard label="Content" value={String(metrics.totalEntries || 0)} sub="tracked entries" icon={FileText} /></div> : <div className="h-40 border border-dashed border-creo-border rounded-lg flex flex-col items-center justify-center text-center px-5"><Lock size={16} className="text-creo-text-muted mb-2" /><p className="creo-body text-creo-text-secondary">Connect a supported analytics source</p><p className="creo-caption text-creo-text-muted mt-1 max-w-md">CRÉO will not manufacture views, watch time, engagement or growth numbers to make the dashboard look impressive.</p></div>}</div><div className="creo-surface rounded-xl p-4"><div className="flex items-center justify-between mb-4"><div><h2 className="creo-h3">Today's focus</h2><p className="creo-caption text-creo-text-muted mt-0.5">A commitment, not a fake score.</p></div><Target size={15} className="text-creo-primary" /></div><div className="flex items-center gap-4"><div className="relative w-16 h-16 flex-shrink-0"><svg viewBox="0 0 64 64" className="w-16 h-16 -rotate-90"><circle cx="32" cy="32" r="27" fill="none" stroke="#1E2124" strokeWidth="5" /><circle cx="32" cy="32" r="27" fill="none" stroke="#E17E4A" strokeWidth="5" strokeLinecap="round" strokeDasharray={`${2 * Math.PI * 27}`} strokeDashoffset={`${2 * Math.PI * 27 * (1 - Math.min(1, genToday / Math.max(1, limit === Infinity ? 1 : limit)))}`} /></svg><span className="absolute inset-0 flex items-center justify-center"><Flame size={14} className="text-creo-primary" /></span></div><div><p className="text-lg font-semibold text-creo-text-primary">{genToday}{limit === Infinity ? '' : ` / ${limit}`}</p><p className="creo-caption text-creo-text-muted">generations today</p></div></div><div className="mt-5 flex items-center gap-2 text-creo-text-secondary creo-caption"><Check size={12} className="text-creo-success" /> {streak ? `${streak}-day streak in progress` : 'Generate today to start your streak'}</div></div></section>
      </div></main>
  </div>;
}
